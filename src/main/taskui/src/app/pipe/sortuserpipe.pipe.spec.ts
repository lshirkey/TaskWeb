import { SortuserpipePipe } from './sortuserpipe.pipe';

describe('SortuserpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortuserpipePipe();
    expect(pipe).toBeTruthy();
  });
});
